# Bash Battle Arena

A 15-level Bash scripting exercise progressing from basic file operations to monitoring, backup rotation, configuration parsing, and interactive menus.

## Structure

Each level includes:

- `levelN.sh` — script solution
- `levelN.md` — explanation and lessons learned